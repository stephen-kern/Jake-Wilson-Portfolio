import React from "react";
import { Link } from "react-router-dom";

const Header = () => {
  return (
    <header>
        <Link to="/"> Jake Wilson </Link>
      <nav>
        
      </nav>
    </header>
  );
}

export default Header;
